# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: ChangeRequest
  connector: simplexml
  connector_options:
    xpath_to_nodes: /changes/ChangeRequest
    style: element_centric

  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('ChangeRequest', key_is_id=True)

    - property: audit_trail
      external_name: audit_trail
      # Type: Blob Stats: 112 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: author
      external_name: author
      # Type: User Stats: 112 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: backout_plan
      external_name: backout_plan
      # Type: Text Stats: 102 properties of this type in this kind.
      import_transform: db.Text

    - property: communication_plan
      external_name: communication_plan
      # Type: Text Stats: 2 properties of this type in this kind.
      import_transform: db.Text

    - property: created_on
      external_name: created_on
      # Type: Date/Time Stats: 112 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: description
      external_name: description
      # Type: Text Stats: 6 properties of this type in this kind.
      import_transform: db.Text

    - property: documentation
      external_name: documentation
      # Type: Text Stats: 102 properties of this type in this kind.
      import_transform: db.Text

    - property: endTime
      external_name: endTime
      # Type: Date/Time Stats: 112 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: impact
      external_name: impact
      # Type: Text Stats: 2 properties of this type in this kind.
      import_transform: db.Text

    - property: implementation_steps
      external_name: implementation_steps
      # Type: Text Stats: 102 properties of this type in this kind.
      import_transform: db.Text

    - property: layman_description
      external_name: layman_description
      # Type: Text Stats: 102 properties of this type in this kind.
      import_transform: db.Text

    - property: priority
      external_name: priority
      # Type: String Stats: 106 properties of this type in this kind.

    - property: rationale
      external_name: rationale
      # Type: Text Stats: 2 properties of this type in this kind.
      import_transform: db.Text

    - property: risks
      external_name: risks
      # Type: Text Stats: 102 properties of this type in this kind.
      import_transform: db.Text

    - property: startTime
      external_name: startTime
      # Type: Date/Time Stats: 112 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: status
      external_name: status
      # Type: String Stats: 112 properties of this type in this kind.

    - property: summary
      external_name: summary
      # Type: Text Stats: 110 properties of this type in this kind.
      import_transform: db.Text

    - property: technician
      external_name: technician
      # Type: String Stats: 112 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

      
    - property: tests_conducted
      external_name: tests_conducted
      # Type: Text Stats: 102 properties of this type in this kind.
      import_transform: db.Text


- kind: UserGroup
  connector: simplexml
  connector_options:
    xpath_to_nodes: /changes/ChangeRequest
    style: element_centric
  
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: members
      external_name: members
      # Type: User Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: name
      external_name: name
      # Type: String Stats: 2 properties of this type in this kind.

    - property: owners
      external_name: owners
      # Type: User Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

